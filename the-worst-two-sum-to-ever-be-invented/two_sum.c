/* This is a horrible solution for Leetcode's problem "Two sum".
 i love GNU Linux
 */

#include <stddef.h>
#include <stdint.h>
#include <stdlib.h>
#include <stdio.h>
#define WHEN_YOU_GAZE_INTO_THE_ABYSS(x) (sizeof(x))
static inline size_t sizeof_int() {
    // Procedural programming!
    return sizeof(int);
}
struct NodeObjectThatIsExtremelyVerboseAndCIsTheBestLanguageBecauseItIsGNU_Linux_andIt_isSO_COOL{
    uintptr_t KeyThatIsSoC0ol;
    uintptr_t ValueThatIsSoCo0l;
};
int*twoSum(int*Strange_numBerArrayThing,int Strange_numBerArrayThing_length,int WeirdTargetSumThing,int*ZeroIfItFailedTwoIfItSucceeded){
    size_t (*WHEN_YOU_GAZE_INTO_THE_ABYSS_THE_ABYSS_GAZES_BACK___)() = sizeof_int;
    int*indices=calloc(2,WHEN_YOU_GAZE_INTO_THE_ABYSS_THE_ABYSS_GAZES_BACK___());
    struct NodeObjectThatIsExtremelyVerboseAndCIsTheBestLanguageBecauseItIsGNU_Linux_andIt_isSO_COOL*MapThatIsSoCool=calloc(Strange_numBerArrayThing_length,WHEN_YOU_GAZE_INTO_THE_ABYSS(struct NodeObjectThatIsExtremelyVerboseAndCIsTheBestLanguageBecauseItIsGNU_Linux_andIt_isSO_COOL));
    int*ReallyCo0lMapIndex=malloc(WHEN_YOU_GAZE_INTO_THE_ABYSS_THE_ABYSS_GAZES_BACK___());
    for(*(int*)ReallyCo0lMapIndex=0;*(int*)ReallyCo0lMapIndex<Strange_numBerArrayThing_length;(*(int*)ReallyCo0lMapIndex)++){ // In strong typing we trust
        MapThatIsSoCool[*(int*)ReallyCo0lMapIndex].KeyThatIsSoC0ol=(uintptr_t)malloc(WHEN_YOU_GAZE_INTO_THE_ABYSS_THE_ABYSS_GAZES_BACK___());
        MapThatIsSoCool[*(int*)ReallyCo0lMapIndex].ValueThatIsSoCo0l=(uintptr_t)malloc(WHEN_YOU_GAZE_INTO_THE_ABYSS_THE_ABYSS_GAZES_BACK___());
    }
    free(ReallyCo0lMapIndex);
    int ReallyCoolMapIndex=0;
    for(int CloudMCCoolioIndex=0;CloudMCCoolioIndex<Strange_numBerArrayThing_length;CloudMCCoolioIndex++){
        int diff=WeirdTargetSumThing-Strange_numBerArrayThing[CloudMCCoolioIndex];
        for(int ReallyC0olMapIndex=0;ReallyC0olMapIndex<ReallyCoolMapIndex;ReallyC0olMapIndex++){
            if(*(int*)(MapThatIsSoCool[ReallyC0olMapIndex].KeyThatIsSoC0ol)==diff){
                indices[0]=CloudMCCoolioIndex;indices[1]=*(int*)(MapThatIsSoCool[ReallyC0olMapIndex].ValueThatIsSoCo0l);
                *(int*)ZeroIfItFailedTwoIfItSucceeded=2;
                ReallyCo0lMapIndex=malloc(WHEN_YOU_GAZE_INTO_THE_ABYSS_THE_ABYSS_GAZES_BACK___());
                for(*(int*)ReallyCo0lMapIndex=0;*(int*)ReallyCo0lMapIndex<Strange_numBerArrayThing_length;(*(int*)ReallyCo0lMapIndex)++){
                    free((void*)MapThatIsSoCool[*(int*)ReallyCo0lMapIndex].KeyThatIsSoC0ol);
                    free((void*)MapThatIsSoCool[*(int*)ReallyCo0lMapIndex].ValueThatIsSoCo0l);
                }
                free(ReallyCo0lMapIndex);
                free(MapThatIsSoCool);
                return indices;
            }
        }
        ReallyCoolMapIndex--;
        *(int*)(MapThatIsSoCool[++ReallyCoolMapIndex].KeyThatIsSoC0ol)=Strange_numBerArrayThing[CloudMCCoolioIndex];
        *(int*)(MapThatIsSoCool[ReallyCoolMapIndex].ValueThatIsSoCo0l)=CloudMCCoolioIndex;
        ReallyCoolMapIndex++;
    }
    ReallyCo0lMapIndex=malloc((*(*(*(*(*(*(*(*(*(*(*(*(*(*(*(*(*(*WHEN_YOU_GAZE_INTO_THE_ABYSS_THE_ABYSS_GAZES_BACK___))))))))))))))))))());
    for(*(int*)ReallyCo0lMapIndex=0;*(int*)ReallyCo0lMapIndex<Strange_numBerArrayThing_length;(*(int*)ReallyCo0lMapIndex)++){
        free((void*)MapThatIsSoCool[*ReallyCo0lMapIndex].KeyThatIsSoC0ol);
        free((void*)MapThatIsSoCool[*ReallyCo0lMapIndex].ValueThatIsSoCo0l);
    }
    free(ReallyCo0lMapIndex);
    free(MapThatIsSoCool);
    *&*&*&*&*&*&*&*&*&*&*&*&*&*&*&*&*&*&*&*&*&*&*&*&*&*&*&*&*&*&*&*&*&*&*&*&*&*&*&*&*&*&*&*&*&*&*&*&*&*&*&*&*&*&*&*&*&*&*&*&*&*&*&*&*&*&*&*&*&*&*&*&*&*&*(int*)ZeroIfItFailedTwoIfItSucceeded=0;
    return NULL;
}
